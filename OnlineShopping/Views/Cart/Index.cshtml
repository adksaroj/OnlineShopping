@model List<OnlineShoppingDataAccess.Products>

@{
    ViewBag.Title = "Index";
}

<h2>My Cart</h2>

@if (Model != null)
{
    <table class="table table-striped table-hover">
        <tr>
            <th></th>
            <th>
                @Html.DisplayNameFor(model => model[0].ProductName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model[0].Category)
            </th>
            <th>
                @Html.DisplayNameFor(model => model[0].Cost)
            </th>
            <th>Actions</th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <img class="img-thumbnail img-responsive" width="150" height="300" src="@Url.Content("~/static/products/")@item.ImageName" alt="@item.ImageName" />
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ProductName)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.Category)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Cost)
                </td>
                <td>
                    @Html.ActionLink("Remove from Cart", "Remove", new { id = item.Id }, htmlAttributes: new { @class = "btn btn-danger" })
                </td>
            </tr>
        }

    </table>
}